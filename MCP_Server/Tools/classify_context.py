def classify_content(message):
    # Classify which cluster (contracts/expenses/people/projects/timesheets)

    # Classify and get the id from the endpoints in the cluster

    # Return that id
    pass